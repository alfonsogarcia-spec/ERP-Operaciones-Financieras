# Puerto del servidor (por defecto 4174)
PORT=4174

# Fase 2 (aún no usado por el MVP): cadena de conexión Supabase/Postgres.
# Cuando esté configurada y /api/estado reporte db:true, el frontend cambiará
# de localStorage a modo API.
# DATABASE_URL=
